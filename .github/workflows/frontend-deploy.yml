name: Frontend Blog - Build and Deploy

on:
  push:
    branches: [ main ]
    paths:
      - 'my-blog/**'
      - '.github/workflows/frontend-deploy.yml'
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      
      - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      - name: Build and push frontend blog
        uses: docker/build-push-action@v4
        with:
          context: ./my-blog
          file: ./my-blog/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/my-personal-blog:latest
            ${{ secrets.DOCKER_USERNAME }}/my-personal-blog:${{ github.sha }}
          cache-from: type=registry,ref=${{ secrets.DOCKER_USERNAME }}/my-personal-blog:buildcache
          cache-to: type=registry,ref=${{ secrets.DOCKER_USERNAME }}/my-personal-blog:buildcache,mode=max

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    
    steps:
      - name: Log deployment start
        run: |
          echo "🚀 开始部署前端博客到生产环境..."
          echo "📦 仓库: ${{ github.repository }}"
          echo "🔗 提交: ${{ github.sha }}"
          echo "⏰ 时间: $(date)"

      - name: Deploy to server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          port: ${{ secrets.SERVER_PORT }}
          script: |
            cd ~/my-blog
            export DOCKER_USERNAME=${{ secrets.DOCKER_USERNAME }}
            
            echo "拉取最新镜像..."
            docker pull $DOCKER_USERNAME/my-personal-blog:latest
            
            echo "停止旧容器..."
            docker stop my-personal-blog 2>/dev/null || true
            docker rm my-personal-blog 2>/dev/null || true
            
            echo "启动新容器..."
            docker run -d \
              --name my-personal-blog \
              --restart always \
              -p 80:80 \
              -v $(pwd)/nginx.conf:/etc/nginx/conf.d/default.conf \
              -v $(pwd)/logs:/var/log/nginx \
              $DOCKER_USERNAME/my-personal-blog:latest
            
            sleep 10
            
            if curl -f http://localhost; then
              echo "✅ 部署成功！"
            else
              echo "❌ 部署失败！"
              exit 1
            fi

      - name: Create deployment issue
        uses: actions/github-script@v6
        with:
          script: |
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: '🚀 前端博客部署完成 - ' + new Date().toLocaleString('zh-CN'),
              body: `
            ## 🎉 前端博客部署成功！
            
            **部署信息：**
            - 📅 部署时间：${new Date().toLocaleString('zh-CN')}
            - 🔗 提交哈希：\`${{ github.sha }}\`
            - 🌐 网站地址：http://你的服务器IP
            - 📝 工作流：[${{ github.workflow }}](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})
            
            **部署状态：** ✅ 成功
            
            ---
            *此 Issue 由前端 CI/CD 流水线自动创建*
              `,
              labels: ['frontend', 'deployment', 'success']
            })
        if: success()

      - name: Create deployment failure issue
        uses: actions/github-script@v6
        with:
          script: |
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: '❌ 前端博客部署失败 - ' + new Date().toLocaleString('zh-CN'),
              body: `
            ## ⚠️ 前端博客部署失败！
            
            **部署信息：**
            - 📅 部署时间：${new Date().toLocaleString('zh-CN')}
            - 🔗 提交哈希：\`${{ github.sha }}\`
            - 📝 工作流：[${{ github.workflow }}](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})
            
            **部署状态：** ❌ 失败
            
            **可能的原因：**
            - 服务器连接问题
            - Docker 镜像拉取失败
            - 容器启动失败
            - 健康检查未通过
            
            **解决方案：**
            1. 检查服务器状态和网络连接
            2. 查看 [GitHub Actions 日志](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})
            3. 手动登录服务器检查 Docker 状态
            4. 重新运行部署工作流
            
            ---
            *此 Issue 由前端 CI/CD 流水线自动创建*
              `,
              labels: ['frontend', 'deployment', 'failure', 'bug']
            })
        if: failure()
